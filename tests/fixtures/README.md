# fixtures

This directory stores binary and textual test fixtures.

Planned usage:

- `gost_a1_*.hex`, `gost_a2_*.hex`, `gost_a3_*.hex`, `gost_a4_*.hex` for official vectors.
- Optional decoded `.json` metadata for expected fields.
