# integration tests

Integration tests are not implemented yet.

Planned scope:

- End-to-end packet flow through `crisp-core` and future `crisp-driver` datapath.
- Control-plane interactions via future `crispctl` commands.
